{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "Compilation (g++)",
            "type": "shell",
            "command": "g++",
            "args": ["-std=c++11", "-Wall", "-I", "include", "main.cpp", "-o", "main.out"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}